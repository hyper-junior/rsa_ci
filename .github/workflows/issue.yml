name: PR

'on':
  issues

jobs:
  check-title:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo 'hello'
      - name: lint
        env:
          PR_TITLE: ${{github.event.issuse.title}}
        run: |
          echo "$PR_TITLE" 
    
      - name: runsh
        run: |
          echo "${{github.event.pull_request.title}}"
